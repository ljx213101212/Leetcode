#include "pch.h"
class Solution {
public:
    int distanceBetweenBusStops(vector<int>& distance, int start, int destination);
    int calculateStartToDest(vector<int>& distance, int start , int destination);
};